{
  "name": "mare-nostrum",
  "version": "1.0.0",
  "description": "Cozy Roman solarpunk life-sim — p5.js browser game",
  "main": "electron.js",
  "scripts": {
    "dev": "npx http-server -p 8080 -c-1 --silent",
    "start": "npx http-server -p 8080 -c-1 -o",
    "electron": "electron electron.js",
    "electron-dev": "electron electron.js --dev",
    "build": "bash build.sh",
    "bundle": "node build.mjs",
    "check": "node -c sketch.js && node -c ui.js && node -c player.js && node -c world.js && node -c events.js && node -c npc.js && node -c fishing.js && node -c farming.js && node -c wreck.js && node -c sound.js && node -c combat.js && node -c diving.js && node -c economy.js && node -c islands.js && node -c narrative.js && node -c progression.js && node -c engine.js && node -c debug.js && node -c cinematics.js && node -c menu.js && node -c mobile.js && echo 'All files OK'",
    "lint": "npx eslint *.js --no-error-on-unmatched-pattern",
    "test": "npx playwright test",
    "test:positions": "node test-positions.js",
    "count": "wc -l *.js *.html",
    "format": "npx prettier --write '*.js' '*.html' --single-quote --print-width 160"
  },
  "keywords": [
    "game",
    "p5js",
    "roman",
    "solarpunk",
    "life-sim",
    "indie"
  ],
  "author": "amidevs",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "electron": "^41.0.3",
    "esbuild": "^0.27.4",
    "eslint": "^10.0.3",
    "http-server": "^14.1.1",
    "live-server": "^1.2.2",
    "prettier": "^3.8.1"
  }
}
